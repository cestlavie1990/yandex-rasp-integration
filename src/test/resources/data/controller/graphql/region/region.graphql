query getRegion($id: ID!){
    region(id: $id) {
        id
        title
        code {
            esrCode
            yandexCode
        }
        country {
            id
        }
        settlements {
            id
        }
    }
}