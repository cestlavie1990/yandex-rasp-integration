query getRegions($filter: RegionFilter){
    regions(filter: $filter) {
        id
        title
        code {
            esrCode
            yandexCode
        }
        country {
            id
        }
    }
}