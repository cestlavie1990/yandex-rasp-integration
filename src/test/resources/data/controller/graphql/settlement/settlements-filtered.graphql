query getSettlements($filter: SettlementFilter){
    settlements(filter: $filter) {
        id
        title
        code {
            esrCode
            yandexCode
        }
        region {
            id
        }
    }
}