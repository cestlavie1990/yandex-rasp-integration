query getSettlement($id: ID!){
    settlement(id: $id) {
        id
        title
        code {
            esrCode
            yandexCode
        }
        region {
            id
        }
        stations {
            id
        }
    }
}