extend type Query {
    country(id: ID!): Country!
    countries(filter: CountryFilter): [Country!]!
}

type Country {
    id: ID!
    code: Code
    title: String
    regions: [Region!]!
}

input CountryFilter {
    id: StringCriteria
    title: StringCriteria
}